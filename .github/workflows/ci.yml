name: CI

on:
  pull_request:
    branches:
      - main
      - dev
  push:
    branches:
      - dev

permissions:
  contents: read

jobs:
  lint-and-test:
    name: Lint and Test
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: certificate_generator_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        if: ${{ hashFiles('backend/package.json') != '' || hashFiles('frontend/package.json') != '' }}
        with:
          node-version: 18

      - name: Install backend dependencies
        if: ${{ hashFiles('backend/package.json') != '' }}
        working-directory: ./backend
        run: npm install

      - name: Install frontend dependencies
        if: ${{ hashFiles('frontend/package.json') != '' }}
        working-directory: ./frontend
        run: npm install

      - name: Lint backend
        if: ${{ hashFiles('backend/package.json') != '' }}
        working-directory: ./backend
        run: npm run lint

      - name: Lint frontend
        if: ${{ hashFiles('frontend/package.json') != '' }}
        working-directory: ./frontend
        run: npm run lint

      - name: Run backend tests
        if: ${{ hashFiles('backend/package.json') != '' }}
        working-directory: ./backend
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/certificate_generator_test
          REDIS_URL: redis://localhost:6379
        run: npm run test

      - name: Run frontend tests
        if: ${{ hashFiles('frontend/package.json') != '' }}
        working-directory: ./frontend
        run: npm run test

  build-check:
    name: Build Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        if: ${{ hashFiles('frontend/package.json') != '' }}
        with:
          node-version: 18

      - name: Install frontend dependencies
        if: ${{ hashFiles('frontend/package.json') != '' }}
        working-directory: ./frontend
        run: npm install

      - name: Build frontend
        if: ${{ hashFiles('frontend/package.json') != '' }}
        working-directory: ./frontend
        run: npm run build
